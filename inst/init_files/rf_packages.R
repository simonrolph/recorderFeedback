# Package manifest for this project.
# Keep this list aligned with packages used in scripts/ and templates/content.Rmd.
# Add every package here first, then mirror package names in `_targets.R` `project_packages`.

library(ggplot2)
library(rmarkdown)
library(tidyr)
library(lubridate)
