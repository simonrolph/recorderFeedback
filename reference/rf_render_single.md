# Render feedback for a single recipient

Generates feedback for one recipient using their data.

## Usage

``` r
rf_render_single(recipient_id, view = F)
```

## Arguments

- recipient_id:

  Integer or character. ID of the recipient.

- view:

  Logical. Whether to show the content in the viewer.

## Value

Path to rendered file.
